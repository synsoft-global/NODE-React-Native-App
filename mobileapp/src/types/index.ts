export interface loginRequest {
  email: string;
  password: string;
}

export interface moviesItemType {
  createdAt: string;
  id: string;
  image: string;
  publishingYear: string;
  title: string;
  updatedAt: string;
  userId: number;
}
